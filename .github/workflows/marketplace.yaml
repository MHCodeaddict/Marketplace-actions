name: Python app
on: push
jobs: 
 build:
  name: buildin a python app
  runs-on: ubuntu-latest
  steps:
  - name: check out the repo
    uses: actions/checkout@v4
  - name: python syntax checker
    uses: cclauss/Find-Python-syntax-errors-action@v0.2.0
